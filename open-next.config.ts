import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // default config is usually enough for Next.js apps
  // you can customize the build output, caching, etc. here
});